# These are local development settings, not production credentials.
API_INTERNAL_URL=http://127.0.0.1:4000
# The backend is not included. See README.md before pointing a build at a service.
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
